# Сверка: вывод gstack ↔ загруженный Vision+PRD (v0.1)

**Дата:** 2026-07-16
**Сверяемое:** `plan-ceo-review-luidor-agent.md` / `office-hours-luidor-agent.md` (мой прогон gstack)
↔ `agent_kreditnogo_analiza_vision_prd.md` (загруженный PRD).

---

## Где совпадаем (подтверждают друг друга)

| Тема | gstack | PRD | Статус |
|------|--------|-----|--------|
| Ядро = чат по сделке | клин A | P0.1 | ✅ идентично |
| Саммари→детали как принцип | PRIN-01 | «саммари → детали», P0.3 | ✅ идентично, оба разрешают конфликт «коротко↔не резать» |
| Периметр / антиутечка | PRIN-02, GOAL-03 | P0.5 (blocking) | ✅ идентично |
| Спрос доказан поведением (ChatGPT-обход) | Demand Reality 🟢 | «Проблема» + «Зачем свой агент» | ✅ идентично |
| Персона = кредитный аналитик по сделке | SEG-01 | Персона | ✅ идентично |
| Не автопринятие решения | PRIN-06 | Не-цель v1 | ✅ идентично |
| Не привязывать к формам | PRIN-05 | Не-цель v1 | ✅ идентично |
| North Star: доля принятых + частота раскрытия | GOAL-02, Q-03 | Метрики (частота раскрытия валидирует принцип) | ✅ PRD богаче — беру его формулировку |
| Отраслевые данные/ТГ/ритейлеры → позже | горизонт B / R2 | P2 | ✅ идентично |
| CF-стресс, финанализ → позже | R1/R2 | P1.2–P1.3 | ✅ идентично |
| Открытые вопросы (ВНД-список, доступ к данным, ИБ-контур) | Q-01,Q-02,Q-05 / T-1,T-2 | таблица «Открытые вопросы» | ✅ идентично |

**Вывод:** по ядру, принципам и большинству фазирования — полное совпадение. Оба документа
пришли к одной лестнице независимо.

---

## Материальное расхождение (одно, но важное)

**Что попадает в v1 по двум пунктам — ВНД и историчность/EWS — расходится.**

| Функция | Мой прогон gstack | Загруженный PRD |
|---------|-------------------|-----------------|
| **Проверка по ВНД + нестандартности** | R1 (отложено) | **P0.4 — в ядре v1** |
| **Историчность / EWS-подсветка (крючок C)** | **R0 — принято (Selective Expansion)** | **P2 — последняя фаза** |

Это прямая инверсия. Причём PRD в разделе «Не-цели v1» **явно** пишет: *«не строим полный
внешний data-контур в v1 (… EWS …) — это следующие фазы»*.

### Кто прав — разбор по зависимостям

- **ВНД (за PRD, против моего R1).** Клиент **сам назвал** это точкой боли («текущий ассистент
  справлялся плохо») → это дифференциатор, а не довесок. Нужен лишь **узкий список документов**,
  который даёт клиент — **низкий интеграционный риск**. Это часть «заключения по сделке», а не
  горизонт. **Моё отложение ВНД в R1 — ошибка приоритизации: недооценил названный клиентом
  дифференциатор. PRD прав, ВНД → в v1.**

- **Историчность/EWS (за PRD, корректирую свой крючок C).** C бьёт в «за что увольняют» и даёт
  вау-демо — но **упирается в блокеры, которые я же и пометил**: T-2 (доступ к истории клиента/группы)
  и T-4 (источники историчности: БКИ, арбитраж, ЕФРСБ). Это **новые интеграции = высокий риск**.
  PRD резонно уводит их в фазу 3. **Мой «C в R0» был движим силой демо, а не готовностью данных.**

**Итог:** расхождение разрешается в пользу PRD. Моя ошибка — я положил в R0 эффектную, но
data-заблокированную функцию (C), а низкорисковый названный клиентом дифференциатор (ВНД) отложил.

---

## Разрешение (рекомендация)

**Реконсилированный v1 (R0):** клин A (чат + короткое заключение) + **P0.4 ВНД/нестандартности**
+ P0.5 периметр — то есть **полный P0 из PRD**.

**Крючок C (историчность/EWS) — условный cherry-pick, не в базовом R0:**
- ЕСЛИ история клиента/группы **уже присутствует в контексте сделки** (без новой интеграции) —
  оставляем лёгкую историческую подсветку как Selective-Expansion cherry-pick.
- ИНАЧЕ (нужны внешние источники) — падает в **P2**, как в PRD.
- Развилка привязана к **T-2**. Это и есть честный Selective Expansion: кандидат принят условно,
  при блокере — в TODOS/P2.

Такое разрешение **сохраняет дух Selective Expansion** (база safe, кандидат — по opt-in и
готовности данных) и **выравнивается с PRD** (P0 = ядро + ВНД; EWS/историчность = P2).

---

## Что правлю в артефактах gstack

1. `plan-ceo-review`: VERDICT → R0 = A + **ВНД(P0.4)** + периметр; C → условный cherry-pick, gate T-2.
2. `TODOS.md`: ВНД поднять из R1 в R0; C пометить «условно R0 / иначе P2».
3. `vision`: changelog 0.3 — реконсиляция с PRD.

PRD принимается как ведущий документ по фазированию; мой прогон gstack — как метод-слой
(диагностика спроса, форсирующие вопросы, режим скоупа) поверх него.
